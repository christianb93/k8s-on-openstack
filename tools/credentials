#
# Source this from the repository root to set up
# kubectl and the openstack client credentials
#
statedir=$(cd .state && pwd)
export KUBECONFIG=$statedir/config/admin-kubeconfig
source $statedir/credentials/k8s-openrc